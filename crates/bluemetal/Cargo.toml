[package]
name = "bluemetal"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "bluemetal"
test = false

[features]

[dependencies]
init = { path = "../init" }
serial = { path = "../serial" }

[build-dependencies]
configure = { path = "../../configure/build" }
