[package]
name = "init"
version = "0.0.0"
edition = "2021"

[lib]
path = "src/lib.rs"
test = false

[dependencies]
panic = { path = "../panic" }
serial = { path = "../serial" }

[build-dependencies]
configure = { path = "../../configure/build" }
